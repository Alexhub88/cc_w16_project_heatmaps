import React from 'react';
import SettingsForm from '../components/SettingsForm.js'

const SettingsContainer = (props) => {

  return (
    <div>
      <SettingsForm
        specs={this.props.mapSpecs}
      />
    </div>
  )}

export default SettingsContainer
